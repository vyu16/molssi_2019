"""
chop.py
A funny repo for the 2019 MolSSI bootcamp

Binary search.
"""


def chop1(target, num_list):
    """
    Searches a number in a sorted list of numbers.

    Parameters
    ----------
    target : integer
        Target number

    num_list : list
        A sorted list of numbers

    Returns
    -------
    id : integer
        Index of target in num_list
    """
    if not isinstance(num_list, list):
        raise TypeError('num_list {} not a list'.format(num_list))

    return -999


def chop2(target, num_list):
    """
    Searches a number in a sorted list of numbers.

    Parameters
    ----------
    target : integer
        Target number

    num_list : list
        A sorted list of numbers

    Returns
    -------
    id : integer
        Index of target in num_list
    """
    if not isinstance(num_list, list):
        raise TypeError('num_list {} not a list'.format(num_list))

    return -999
